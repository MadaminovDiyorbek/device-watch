name: device_watch_app
description: DeviceWatch mobile agent
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.3
  device_info_plus: ^11.2.0
  battery_plus: ^6.2.1
  connectivity_plus: ^6.1.1
  network_info_plus: ^6.1.4
  system_info2: ^4.1.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
